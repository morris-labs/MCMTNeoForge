--- a/net/minecraft/world/entity/ai/village/poi/PoiManager.java
+++ b/net/minecraft/world/entity/ai/village/poi/PoiManager.java
@@ -64,19 +_,29 @@
         this.distanceTracker = new PoiManager.DistanceTracker();
     }
 
-    public void add(BlockPos p_217920_, Holder<PoiType> p_217921_) {
-        this.getOrCreate(SectionPos.asLong(p_217920_)).add(p_217920_, p_217921_);
+    public void add(BlockPos p_217920_, Holder<PoiType> p_217921_) { // MCMT: write lock
+        this.mcmtLock.writeLock().lock();
+        try {
+            this.getOrCreate(SectionPos.asLong(p_217920_)).add(p_217920_, p_217921_);
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
     }
 
-    public void remove(BlockPos p_27080_) {
-        this.getOrLoad(SectionPos.asLong(p_27080_)).ifPresent(p_148657_ -> p_148657_.remove(p_27080_));
+    public void remove(BlockPos p_27080_) { // MCMT: write lock
+        this.mcmtLock.writeLock().lock();
+        try {
+            this.getOrLoad(SectionPos.asLong(p_27080_)).ifPresent(p_148657_ -> p_148657_.remove(p_27080_));
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
     }
 
     public long getCountInRange(Predicate<Holder<PoiType>> p_27122_, BlockPos p_27123_, int p_27124_, PoiManager.Occupancy p_27125_) {
         return this.getInRange(p_27122_, p_27123_, p_27124_, p_27125_).count();
     }
 
-    public boolean existsAtPosition(ResourceKey<PoiType> p_217875_, BlockPos p_217876_) {
+    public boolean existsAtPosition(ResourceKey<PoiType> p_217875_, BlockPos p_217876_) { // MCMT: locking handled by exists
         return this.exists(p_217876_, p_217879_ -> p_217879_.is(p_217875_));
     }
 
@@ -95,11 +_,80 @@
 
     @VisibleForDebug
     public Stream<PoiRecord> getInChunk(Predicate<Holder<PoiType>> p_27118_, ChunkPos p_27119_, PoiManager.Occupancy p_27120_) {
-        return IntStream.range(this.levelHeightAccessor.getMinSection(), this.levelHeightAccessor.getMaxSection())
-            .boxed()
-            .map(p_217886_ -> this.getOrLoad(SectionPos.of(p_27119_, p_217886_).asLong()))
-            .filter(Optional::isPresent)
-            .flatMap(p_217942_ -> p_217942_.get().getRecords(p_27118_, p_27120_));
+        // MCMT: materialise every matching record into a list under ONE read-lock acquire. This runs per
+        // villager per tick from many workers, so the ReentrantReadWriteLock acquire cost (a ThreadLocal.get
+        // plus a CAS on shared state) has to be paid once, not once per section -- an earlier version that
+        // called getOrLoad in the section loop regressed 32-worker scaling badly (MCMT-PLAN §4n). getWhileLocked
+        // reads storage directly while the read lock is held; a never-read column is a cold miss, loaded with
+        // its own getOrLoad/read-lock pair afterwards (does not happen after warmup). Downstream getInSquare /
+        // getInRange / findClosest / take filter this materialised list and never touch a PoiSection map off-lock.
+        int i = this.levelHeightAccessor.getMinSection();
+        int j = this.levelHeightAccessor.getMaxSection();
+        List<PoiRecord> list = new java.util.ArrayList<>();
+        List<Long> misses = null;
+
+        this.mcmtLock.readLock().lock();
+        try {
+            for (int k = i; k < j; k++) {
+                long l = SectionPos.of(p_27119_, k).asLong();
+                Optional<PoiSection> optional = this.getWhileLocked(l);
+                if (optional == null) {
+                    if (misses == null) {
+                        misses = new java.util.ArrayList<>();
+                    }
+                    misses.add(l);
+                } else {
+                    optional.ifPresent(p_mcmt_ -> p_mcmt_.getRecords(p_27118_, p_27120_).forEach(list::add));
+                }
+            }
+        } finally {
+            this.mcmtLock.readLock().unlock();
+        }
+
+        if (misses != null) {
+            for (long l : misses) {
+                Optional<PoiSection> optional = this.getOrLoad(l);
+                if (optional.isPresent()) {
+                    this.mcmtLock.readLock().lock();
+                    try {
+                        optional.get().getRecords(p_27118_, p_27120_).forEach(list::add);
+                    } finally {
+                        this.mcmtLock.readLock().unlock();
+                    }
+                }
+            }
+        }
+
+        return list.stream();
+    }
+
+    /**
+     * MCMT: runs {@code fn} against the POI section for {@code key} under a single read-lock acquire, returning
+     * {@code absent} when the section holds no data. Loads the column (write lock) only when it was never read,
+     * which does not happen after warmup. This keeps the hot villager-sensor reads -- {@code exists},
+     * {@code getType}, {@code getFreeTickets} -- to one lock acquire each. See MCMT-PLAN §4n.
+     */
+    private <T> T mcmtWithSection(long p_mcmt_key_, T p_mcmt_absent_, java.util.function.Function<PoiSection, T> p_mcmt_fn_) {
+        this.mcmtLock.readLock().lock();
+        try {
+            Optional<PoiSection> optional = this.getWhileLocked(p_mcmt_key_);
+            if (optional != null) {
+                return optional.isPresent() ? p_mcmt_fn_.apply(optional.get()) : p_mcmt_absent_;
+            }
+        } finally {
+            this.mcmtLock.readLock().unlock();
+        }
+
+        Optional<PoiSection> optional = this.getOrLoad(p_mcmt_key_);
+        if (optional.isEmpty()) {
+            return p_mcmt_absent_;
+        }
+        this.mcmtLock.readLock().lock();
+        try {
+            return p_mcmt_fn_.apply(optional.get());
+        } finally {
+            this.mcmtLock.readLock().unlock();
+        }
     }
 
     public Stream<BlockPos> findAll(
@@ -152,17 +_,24 @@
             .min(Comparator.comparingDouble(p_217918_ -> p_217918_.distSqr(p_148661_)));
     }
 
-    public Optional<BlockPos> take(Predicate<Holder<PoiType>> p_217947_, BiPredicate<Holder<PoiType>, BlockPos> p_217948_, BlockPos p_217949_, int p_217950_) {
-        return this.getInRange(p_217947_, p_217949_, p_217950_, PoiManager.Occupancy.HAS_SPACE)
-            .filter(p_217934_ -> p_217948_.test(p_217934_.getPoiType(), p_217934_.getPos()))
-            .findFirst()
-            .map(p_217881_ -> {
-                p_217881_.acquireTicket();
-                return p_217881_.getPos();
-            });
+    public Optional<BlockPos> take( // MCMT: write lock (mutates ticket counts)
+        Predicate<Holder<PoiType>> p_217947_, BiPredicate<Holder<PoiType>, BlockPos> p_217948_, BlockPos p_217949_, int p_217950_
+    ) {
+        this.mcmtLock.writeLock().lock();
+        try {
+            return this.getInRange(p_217947_, p_217949_, p_217950_, PoiManager.Occupancy.HAS_SPACE)
+                .filter(p_217934_ -> p_217948_.test(p_217934_.getPoiType(), p_217934_.getPos()))
+                .findFirst()
+                .map(p_217881_ -> {
+                    p_217881_.acquireTicket();
+                    return p_217881_.getPos();
+                });
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
     }
 
-    public Optional<BlockPos> getRandom(
+    public Optional<BlockPos> getRandom( // MCMT: write lock
         Predicate<Holder<PoiType>> p_217952_,
         Predicate<BlockPos> p_217953_,
         PoiManager.Occupancy p_217954_,
@@ -170,73 +_,113 @@
         int p_217956_,
         RandomSource p_217957_
     ) {
-        List<PoiRecord> list = Util.toShuffledList(this.getInRange(p_217952_, p_217955_, p_217956_, p_217954_), p_217957_);
-        return list.stream().filter(p_217945_ -> p_217953_.test(p_217945_.getPos())).findFirst().map(PoiRecord::getPos);
-    }
-
-    public boolean release(BlockPos p_27155_) {
-        return this.getOrLoad(SectionPos.asLong(p_27155_))
-            .map(p_217993_ -> p_217993_.release(p_27155_))
-            .orElseThrow(() -> Util.pauseInIde(new IllegalStateException("POI never registered at " + p_27155_)));
-    }
-
-    public boolean exists(BlockPos p_27092_, Predicate<Holder<PoiType>> p_27093_) {
-        return this.getOrLoad(SectionPos.asLong(p_27092_)).map(p_217925_ -> p_217925_.exists(p_27092_, p_27093_)).orElse(false);
-    }
-
-    public Optional<Holder<PoiType>> getType(BlockPos p_27178_) {
-        return this.getOrLoad(SectionPos.asLong(p_27178_)).flatMap(p_217974_ -> p_217974_.getType(p_27178_));
+        this.mcmtLock.writeLock().lock();
+        try {
+            List<PoiRecord> list = Util.toShuffledList(this.getInRange(p_217952_, p_217955_, p_217956_, p_217954_), p_217957_);
+            return list.stream().filter(p_217945_ -> p_217953_.test(p_217945_.getPos())).findFirst().map(PoiRecord::getPos);
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
+    }
+
+    public boolean release(BlockPos p_27155_) { // MCMT: write lock (mutates ticket counts)
+        this.mcmtLock.writeLock().lock();
+        try {
+            return this.getOrLoad(SectionPos.asLong(p_27155_))
+                .map(p_217993_ -> p_217993_.release(p_27155_))
+                .orElseThrow(() -> Util.pauseInIde(new IllegalStateException("POI never registered at " + p_27155_)));
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
+    }
+
+    public boolean exists(BlockPos p_27092_, Predicate<Holder<PoiType>> p_27093_) { // MCMT: one read-lock acquire (§4n)
+        return this.mcmtWithSection(SectionPos.asLong(p_27092_), Boolean.FALSE, p_mcmt_ -> p_mcmt_.exists(p_27092_, p_27093_));
+    }
+
+    public Optional<Holder<PoiType>> getType(BlockPos p_27178_) { // MCMT: one read-lock acquire (§4n)
+        return this.mcmtWithSection(SectionPos.asLong(p_27178_), Optional.<Holder<PoiType>>empty(), p_mcmt_ -> p_mcmt_.getType(p_27178_));
     }
 
     @Deprecated
     @VisibleForDebug
-    public int getFreeTickets(BlockPos p_148654_) {
-        return this.getOrLoad(SectionPos.asLong(p_148654_)).map(p_217912_ -> p_217912_.getFreeTickets(p_148654_)).orElse(0);
-    }
-
-    public int sectionsToVillage(SectionPos p_27099_) {
-        this.distanceTracker.runAllUpdates();
-        return this.distanceTracker.getLevel(p_27099_.asLong());
-    }
-
-    boolean isVillageCenter(long p_27198_) {
-        Optional<PoiSection> optional = this.get(p_27198_);
-        return optional == null
-            ? false
-            : optional.<Boolean>map(
-                    p_217883_ -> p_217883_.getRecords(p_217927_ -> p_217927_.is(PoiTypeTags.VILLAGE), PoiManager.Occupancy.IS_OCCUPIED).findAny().isPresent()
-                )
-                .orElse(false);
-    }
-
-    @Override
-    public void tick(BooleanSupplier p_27105_) {
-        super.tick(p_27105_);
-        this.distanceTracker.runAllUpdates();
-    }
-
-    @Override
-    protected void setDirty(long p_27036_) {
-        super.setDirty(p_27036_);
-        this.distanceTracker.update(p_27036_, this.distanceTracker.getLevelFromSource(p_27036_), false);
-    }
-
-    @Override
-    protected void onSectionLoad(long p_27145_) {
-        this.distanceTracker.update(p_27145_, this.distanceTracker.getLevelFromSource(p_27145_), false);
-    }
-
-    public void checkConsistencyWithBlocks(SectionPos p_281731_, LevelChunkSection p_281893_) {
-        Util.ifElse(this.getOrLoad(p_281731_.asLong()), p_217898_ -> p_217898_.refresh(p_217967_ -> {
+    public int getFreeTickets(BlockPos p_148654_) { // MCMT: one read-lock acquire (§4n)
+        return this.mcmtWithSection(SectionPos.asLong(p_148654_), Integer.valueOf(0), p_mcmt_ -> p_mcmt_.getFreeTickets(p_148654_));
+    }
+
+    public int sectionsToVillage(SectionPos p_27099_) { // MCMT: write lock (distanceTracker.runAllUpdates mutates)
+        this.mcmtLock.writeLock().lock();
+        try {
+            this.distanceTracker.runAllUpdates();
+            return this.distanceTracker.getLevel(p_27099_.asLong());
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
+    }
+
+    boolean isVillageCenter(long p_27198_) { // MCMT: read lock
+        this.mcmtLock.readLock().lock();
+        try {
+            Optional<PoiSection> optional = this.get(p_27198_);
+            return optional == null
+                ? false
+                : optional.<Boolean>map(
+                        p_217883_ -> p_217883_.getRecords(p_217927_ -> p_217927_.is(PoiTypeTags.VILLAGE), PoiManager.Occupancy.IS_OCCUPIED).findAny().isPresent()
+                    )
+                    .orElse(false);
+        } finally {
+            this.mcmtLock.readLock().unlock();
+        }
+    }
+
+    @Override
+    public void tick(BooleanSupplier p_27105_) { // MCMT: write lock
+        this.mcmtLock.writeLock().lock();
+        try {
+            super.tick(p_27105_);
+            this.distanceTracker.runAllUpdates();
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
+    }
+
+    @Override
+    protected void setDirty(long p_27036_) { // MCMT: write lock
+        this.mcmtLock.writeLock().lock();
+        try {
+            super.setDirty(p_27036_);
+            this.distanceTracker.update(p_27036_, this.distanceTracker.getLevelFromSource(p_27036_), false);
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
+    }
+
+    @Override
+    protected void onSectionLoad(long p_27145_) { // MCMT: write lock
+        this.mcmtLock.writeLock().lock();
+        try {
+            this.distanceTracker.update(p_27145_, this.distanceTracker.getLevelFromSource(p_27145_), false);
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
+    }
+
+    public void checkConsistencyWithBlocks(SectionPos p_281731_, LevelChunkSection p_281893_) { // MCMT: write lock
+        this.mcmtLock.writeLock().lock();
+        try {
+            Util.ifElse(this.getOrLoad(p_281731_.asLong()), p_217898_ -> p_217898_.refresh(p_217967_ -> {
+                    if (mayHavePoi(p_281893_)) {
+                        this.updateFromSection(p_281893_, p_281731_, p_217967_);
+                    }
+                }), () -> {
                 if (mayHavePoi(p_281893_)) {
-                    this.updateFromSection(p_281893_, p_281731_, p_217967_);
+                    PoiSection poisection = this.getOrCreate(p_281731_.asLong());
+                    this.updateFromSection(p_281893_, p_281731_, poisection::add);
                 }
-            }), () -> {
-            if (mayHavePoi(p_281893_)) {
-                PoiSection poisection = this.getOrCreate(p_281731_.asLong());
-                this.updateFromSection(p_281893_, p_281731_, poisection::add);
-            }
-        });
+            });
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
     }
 
     private static boolean mayHavePoi(LevelChunkSection p_27061_) {
@@ -258,16 +_,30 @@
     }
 
     public void ensureLoadedAndValid(LevelReader p_27057_, BlockPos p_27058_, int p_27059_) {
-        SectionPos.aroundChunk(
-                new ChunkPos(p_27058_), Math.floorDiv(p_27059_, 16), this.levelHeightAccessor.getMinSection(), this.levelHeightAccessor.getMaxSection()
-            )
-            .map(p_217979_ -> Pair.of(p_217979_, this.getOrLoad(p_217979_.asLong())))
-            .filter(p_217963_ -> !p_217963_.getSecond().map(PoiSection::isValid).orElse(false))
-            .map(p_217891_ -> p_217891_.getFirst().chunk())
-            .filter(p_217961_ -> this.loadedChunks.add(p_217961_.toLong()))
-            .forEach(p_330057_ -> p_27057_.getChunk(p_330057_.x, p_330057_.z, ChunkStatus.EMPTY));
+        // MCMT: pick the chunks under the write lock, then load them without it. getChunk can take
+        // mcmtChunkLoadLock, and holding mcmtLock across it would invert lock order against the chunk-load
+        // pump, which calls checkConsistencyWithBlocks (mcmtLock) while holding mcmtChunkLoadLock. See §4i/§4n.
+        List<ChunkPos> toload;
+        this.mcmtLock.writeLock().lock();
+        try {
+            toload = SectionPos.aroundChunk(
+                    new ChunkPos(p_27058_), Math.floorDiv(p_27059_, 16), this.levelHeightAccessor.getMinSection(), this.levelHeightAccessor.getMaxSection()
+                )
+                .map(p_217979_ -> Pair.of(p_217979_, this.getOrLoad(p_217979_.asLong())))
+                .filter(p_217963_ -> !p_217963_.getSecond().map(PoiSection::isValid).orElse(false))
+                .map(p_217891_ -> p_217891_.getFirst().chunk())
+                .filter(p_217961_ -> this.loadedChunks.add(p_217961_.toLong()))
+                .collect(java.util.stream.Collectors.toList());
+        } finally {
+            this.mcmtLock.writeLock().unlock();
+        }
+
+        toload.forEach(p_330057_ -> p_27057_.getChunk(p_330057_.x, p_330057_.z, ChunkStatus.EMPTY));
     }
 
+    // MCMT: not independently locked. Every entry point (runAllUpdates via tick / sectionsToVillage, update via
+    // setDirty / onSectionLoad) is a write-locked PoiManager method, so its Long2ByteOpenHashMap and the
+    // SectionTracker queues are only ever touched under mcmtLock's write lock. See MCMT-PLAN §4n.
     final class DistanceTracker extends SectionTracker {
         private final Long2ByteMap levels = new Long2ByteOpenHashMap();
 
