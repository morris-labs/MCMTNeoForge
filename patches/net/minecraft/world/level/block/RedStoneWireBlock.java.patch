--- a/net/minecraft/world/level/block/RedStoneWireBlock.java
+++ b/net/minecraft/world/level/block/RedStoneWireBlock.java
@@ -85,7 +_,16 @@
     });
     private static final float PARTICLE_DENSITY = 0.2F;
     private final BlockState crossState;
-    private boolean shouldSignal = true;
+    // MCMT: `shouldSignal` used to be a plain instance field on this singleton, toggled false around the
+    // getBestNeighborSignal call in calculateTargetStrength below and read back a few lines later by
+    // getSignal, getDirectSignal and isSignalSource. RedStoneWireBlock is one instance shared by every
+    // level, and H1 ticks levels in parallel, so one worker's "exclude my own wire from this computation"
+    // toggle could flip another worker's read for a completely unrelated wire in a different dimension.
+    // Within a single level the toggle-then-read sequence is safe, because vanilla's scheduled-tick and
+    // neighbor-changed processing there is serial -- the hazard is strictly cross-level. A ThreadLocal
+    // confines the flag to the calling worker's own call stack, which is all this code ever needed:
+    // nothing reads another thread's suppression state by design.
+    private static final ThreadLocal<Boolean> SHOULD_SIGNAL = ThreadLocal.withInitial(() -> true);
 
     @Override
     public MapCodec<RedStoneWireBlock> codec() {
@@ -253,7 +_,7 @@
         BlockState blockstate = p_55523_.getBlockState(blockpos);
         if (p_55526_) {
             boolean flag = blockstate.getBlock() instanceof TrapDoorBlock || this.canSurviveOn(p_55523_, blockpos, blockstate);
-            if (flag && shouldConnectTo(p_55523_.getBlockState(blockpos.above()))) {
+            if (flag && p_55523_.getBlockState(blockpos.above()).canRedstoneConnectTo(p_55523_, blockpos.above(), null)) {
                 if (blockstate.isFaceSturdy(p_55523_, blockpos, p_55525_.getOpposite())) {
                     return RedstoneSide.UP;
                 }
@@ -262,10 +_,14 @@
             }
         }
 
-        return !shouldConnectTo(blockstate, p_55525_)
-                && (blockstate.isRedstoneConductor(p_55523_, blockpos) || !shouldConnectTo(p_55523_.getBlockState(blockpos.below())))
-            ? RedstoneSide.NONE
-            : RedstoneSide.SIDE;
+        if (blockstate.canRedstoneConnectTo(p_55523_, blockpos, p_55525_)) {
+             return RedstoneSide.SIDE;
+        } else if (blockstate.isRedstoneConductor(p_55523_, blockpos)) {
+             return RedstoneSide.NONE;
+        } else {
+             BlockPos blockPosBelow = blockpos.below();
+             return p_55523_.getBlockState(blockPosBelow).canRedstoneConnectTo(p_55523_, blockPosBelow, null) ? RedstoneSide.SIDE : RedstoneSide.NONE;
+        }
     }
 
     @Override
@@ -300,9 +_,9 @@
     }
 
     private int calculateTargetStrength(Level p_55528_, BlockPos p_55529_) {
-        this.shouldSignal = false;
+        SHOULD_SIGNAL.set(false);
         int i = p_55528_.getBestNeighborSignal(p_55529_);
-        this.shouldSignal = true;
+        SHOULD_SIGNAL.set(true);
         int j = 0;
         if (i < 15) {
             for (Direction direction : Direction.Plane.HORIZONTAL) {
@@ -392,12 +_,12 @@
 
     @Override
     protected int getDirectSignal(BlockState p_55625_, BlockGetter p_55626_, BlockPos p_55627_, Direction p_55628_) {
-        return !this.shouldSignal ? 0 : p_55625_.getSignal(p_55626_, p_55627_, p_55628_);
+        return !SHOULD_SIGNAL.get() ? 0 : p_55625_.getSignal(p_55626_, p_55627_, p_55628_);
     }
 
     @Override
     protected int getSignal(BlockState p_55549_, BlockGetter p_55550_, BlockPos p_55551_, Direction p_55552_) {
-        if (this.shouldSignal && p_55552_ != Direction.DOWN) {
+        if (SHOULD_SIGNAL.get() && p_55552_ != Direction.DOWN) {
             int i = p_55549_.getValue(POWER);
             if (i == 0) {
                 return 0;
@@ -429,7 +_,7 @@
 
     @Override
     protected boolean isSignalSource(BlockState p_55636_) {
-        return this.shouldSignal;
+        return SHOULD_SIGNAL.get();
     }
 
     public static int getColorForPower(int p_55607_) {
