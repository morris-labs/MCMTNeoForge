--- a/net/minecraft/world/level/entity/EntityLookup.java
+++ b/net/minecraft/world/level/entity/EntityLookup.java
@@ -16,8 +_,20 @@
     private final Int2ObjectMap<T> byId = new Int2ObjectLinkedOpenHashMap<>();
     private final Map<UUID, T> byUuid = Maps.newHashMap();
 
+    // MCMT hook H2: byId and byUuid are plain non-concurrent maps, written when an entity starts or stops being
+    // tracked (under PersistentEntitySectionManager's structural lock) and read on every Level.getEntity and
+    // every unbounded Level.getEntities -- both of which run on entity-tick workers under MCMT. The monitor makes
+    // each access atomic; getEntities and getAllEntities copy the id map's values out under it so the caller
+    // iterates a snapshot with the lock released, exactly as EntitySectionStorage does.
+    private final Object mcmtLock = new Object();
+
     public <U extends T> void getEntities(EntityTypeTest<T, U> p_261575_, AbortableIterationConsumer<U> p_261925_) {
-        for (T t : this.byId.values()) {
+        java.util.List<T> snapshot;
+        synchronized (this.mcmtLock) { // MCMT hook H2
+            snapshot = new java.util.ArrayList<>(this.byId.values());
+        }
+
+        for (T t : snapshot) {
             U u = (U)p_261575_.tryCast(t);
             if (u != null && p_261925_.accept(u).shouldAbort()) {
                 return;
@@ -26,35 +_,47 @@
     }
 
     public Iterable<T> getAllEntities() {
-        return Iterables.unmodifiableIterable(this.byId.values());
+        synchronized (this.mcmtLock) { // MCMT hook H2
+            return Iterables.unmodifiableIterable(new java.util.ArrayList<>(this.byId.values()));
+        }
     }
 
     public void add(T p_156815_) {
-        UUID uuid = p_156815_.getUUID();
-        if (this.byUuid.containsKey(uuid)) {
-            LOGGER.warn("Duplicate entity UUID {}: {}", uuid, p_156815_);
-        } else {
-            this.byUuid.put(uuid, p_156815_);
-            this.byId.put(p_156815_.getId(), p_156815_);
+        synchronized (this.mcmtLock) { // MCMT hook H2
+            UUID uuid = p_156815_.getUUID();
+            if (this.byUuid.containsKey(uuid)) {
+                LOGGER.warn("Duplicate entity UUID {}: {}", uuid, p_156815_);
+            } else {
+                this.byUuid.put(uuid, p_156815_);
+                this.byId.put(p_156815_.getId(), p_156815_);
+            }
         }
     }
 
     public void remove(T p_156823_) {
-        this.byUuid.remove(p_156823_.getUUID());
-        this.byId.remove(p_156823_.getId());
+        synchronized (this.mcmtLock) { // MCMT hook H2
+            this.byUuid.remove(p_156823_.getUUID());
+            this.byId.remove(p_156823_.getId());
+        }
     }
 
     @Nullable
     public T getEntity(int p_156813_) {
-        return this.byId.get(p_156813_);
+        synchronized (this.mcmtLock) { // MCMT hook H2
+            return this.byId.get(p_156813_);
+        }
     }
 
     @Nullable
     public T getEntity(UUID p_156820_) {
-        return this.byUuid.get(p_156820_);
+        synchronized (this.mcmtLock) { // MCMT hook H2
+            return this.byUuid.get(p_156820_);
+        }
     }
 
     public int count() {
-        return this.byUuid.size();
+        synchronized (this.mcmtLock) { // MCMT hook H2
+            return this.byUuid.size();
+        }
     }
 }
