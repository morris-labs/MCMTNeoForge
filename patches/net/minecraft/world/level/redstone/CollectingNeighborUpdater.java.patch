--- a/net/minecraft/world/level/redstone/CollectingNeighborUpdater.java
+++ b/net/minecraft/world/level/redstone/CollectingNeighborUpdater.java
@@ -16,9 +_,28 @@
     private static final Logger LOGGER = LogUtils.getLogger();
     private final Level level;
     private final int maxChainedNeighborUpdates;
-    private final ArrayDeque<CollectingNeighborUpdater.NeighborUpdates> stack = new ArrayDeque<>();
-    private final List<CollectingNeighborUpdater.NeighborUpdates> addedThisLayer = new ArrayList<>();
-    private int count = 0;
+    // MCMT hook: one cascade per thread.
+    //
+    // These three fields are the state of a single in-progress neighbour-update cascade: the first caller
+    // pushes its update and drains the stack, while anything raised during that drain is collected into
+    // addedThisLayer and picked up by the same loop. That is a correct and cheap way to make the cascade
+    // iterative rather than recursive, and it is entirely unsafe once two tick workers share a level.
+    //
+    // The failure is quiet rather than loud. A second worker calling in mid-cascade sees count > 0, decides it
+    // is a nested update, appends to the other worker's layer and returns without running anything. Its update
+    // is then executed on the wrong thread at the wrong point in the wrong cascade, or dropped entirely if
+    // that cascade has already finished. Redstone simply fails to propagate.
+    //
+    // Giving each thread its own cascade restores vanilla's semantics exactly, per thread: a worker's own
+    // nested updates still fold into its own layer and still respect its own chain limit. Two workers updating
+    // the same blocks remain a hazard, but that is what chunk locks are for, not this.
+    private final ThreadLocal<CollectingNeighborUpdater.Cascade> cascade = ThreadLocal.withInitial(CollectingNeighborUpdater.Cascade::new);
+
+    private static final class Cascade {
+        final ArrayDeque<CollectingNeighborUpdater.NeighborUpdates> stack = new ArrayDeque<>();
+        final List<CollectingNeighborUpdater.NeighborUpdates> addedThisLayer = new ArrayList<>();
+        int count = 0;
+    }
 
     public CollectingNeighborUpdater(Level p_230643_, int p_230644_) {
         this.level = p_230643_;
@@ -50,45 +_,46 @@
     }
 
     private void addAndRun(BlockPos p_230661_, CollectingNeighborUpdater.NeighborUpdates p_230662_) {
-        boolean flag = this.count > 0;
-        boolean flag1 = this.maxChainedNeighborUpdates >= 0 && this.count >= this.maxChainedNeighborUpdates;
-        this.count++;
+        CollectingNeighborUpdater.Cascade cascade = this.cascade.get(); // MCMT hook: see the field
+        boolean flag = cascade.count > 0;
+        boolean flag1 = this.maxChainedNeighborUpdates >= 0 && cascade.count >= this.maxChainedNeighborUpdates;
+        cascade.count++;
         if (!flag1) {
             if (flag) {
-                this.addedThisLayer.add(p_230662_);
+                cascade.addedThisLayer.add(p_230662_);
             } else {
-                this.stack.push(p_230662_);
+                cascade.stack.push(p_230662_);
             }
-        } else if (this.count - 1 == this.maxChainedNeighborUpdates) {
+        } else if (cascade.count - 1 == this.maxChainedNeighborUpdates) {
             LOGGER.error("Too many chained neighbor updates. Skipping the rest. First skipped position: " + p_230661_.toShortString());
         }
 
         if (!flag) {
-            this.runUpdates();
+            this.runUpdates(cascade);
         }
     }
 
-    private void runUpdates() {
+    private void runUpdates(CollectingNeighborUpdater.Cascade cascade) {
         try {
-            while (!this.stack.isEmpty() || !this.addedThisLayer.isEmpty()) {
-                for (int i = this.addedThisLayer.size() - 1; i >= 0; i--) {
-                    this.stack.push(this.addedThisLayer.get(i));
+            while (!cascade.stack.isEmpty() || !cascade.addedThisLayer.isEmpty()) {
+                for (int i = cascade.addedThisLayer.size() - 1; i >= 0; i--) {
+                    cascade.stack.push(cascade.addedThisLayer.get(i));
                 }
 
-                this.addedThisLayer.clear();
-                CollectingNeighborUpdater.NeighborUpdates collectingneighborupdater$neighborupdates = this.stack.peek();
+                cascade.addedThisLayer.clear();
+                CollectingNeighborUpdater.NeighborUpdates collectingneighborupdater$neighborupdates = cascade.stack.peek();
 
-                while (this.addedThisLayer.isEmpty()) {
+                while (cascade.addedThisLayer.isEmpty()) {
                     if (!collectingneighborupdater$neighborupdates.runNext(this.level)) {
-                        this.stack.pop();
+                        cascade.stack.pop();
                         break;
                     }
                 }
             }
         } finally {
-            this.stack.clear();
-            this.addedThisLayer.clear();
-            this.count = 0;
+            cascade.stack.clear();
+            cascade.addedThisLayer.clear();
+            cascade.count = 0;
         }
     }
 
